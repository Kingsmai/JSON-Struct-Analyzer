2025.04.15 - 0.2.0

修改说明：

数据结构调整： data_pattern 中的每个路径现在对应一个条目列表，每个条目记录一种数据类型的统计信息。例如：

python
复制
data_pattern = {
    'path1': [
        {'type': 'str', 'count': 5, 'max_length': 10, ...},
        {'type': 'null', 'count': 2}
    ],
    ...
}
类型感知统计： 新增 create_entry 和 update_entry 函数，分别用于初始化新类型条目和更新现有条目统计。每个条目根据值的类型独立维护统计信息（如最大值、最小值、可能值等）。

递归处理优化： 在处理嵌套结构（字典、列表）时，递归生成子路径的逻辑保持不变，确保所有可能的结构都被分析，无论父路径存在哪些类型。

后处理改进： 将类型对象转换为类型名称（如 type(None) 转为 'null'），并将集合转换为列表以确保 JSON 序列化兼容性。